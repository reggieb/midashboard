<h1>
  <%= %w{new create}.include?(action_name) ? 'Create' : 'Update' %>
  User
</h1>

<%= simple_form_for(@user) do |f| %>
  <%= f.error_notification %>
  <%= f.input :email %>
  <%= f.input :password %>
  <%= f.input :password_confirmation %>
  <%= f.button :submit %>

<% end %>
